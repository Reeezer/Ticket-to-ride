<UserControl x:Class="Ticket_to_ride.View.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:enums="clr-namespace:Ticket_to_ride.Enums"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" >
    <Control.Resources>
        <ObjectDataProvider x:Key="playerColors" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:PlayerColor"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Control.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="100"/>
            <ColumnDefinition Width="auto" MinWidth="70"/>
        </Grid.ColumnDefinitions>

        <TextBox
            Grid.Column="0"
            Margin="0 0 16 0"
            Text="{Binding Name}"/>
        <ComboBox 
            Grid.Column="1"
            Name="colorBox"
            SelectedItem="{Binding Color}">
        </ComboBox>
    </Grid>
</UserControl>
